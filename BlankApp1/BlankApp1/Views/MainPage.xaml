<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BlankApp1.Views.MainPage"
             Title="{Binding Title}">

    <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
        <Label Text="{Binding MyQuestion}}" />
        <Entry Keyboard="Numeric" Text="{Binding MyAnswer}"></Entry>
        <Label TextColor="Red" Text="{Binding MyResult}"></Label>
        <Button Text="送出答案" Command="{Binding SendAnswer}"></Button>
        <Button Text="重新產生問題" Command="{Binding ReGen}"></Button>
    </StackLayout>

</ContentPage>